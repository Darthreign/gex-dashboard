# Optionnel — uniquement pour le backfill historique (gex/backfill.py).
# Le dashboard live (run.py) n'en a pas besoin : les données CBOE sont
# publiques et sans clé. Compte requis sur https://databento.com (payant,
# facturation à la donnée — le script affiche un devis avant tout
# téléchargement et respecte --max-cost).
DATABENTO_API_KEY=db-xxxxxxxxxxxxxxxxxxxxxxxxxx
